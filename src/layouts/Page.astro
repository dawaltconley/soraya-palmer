---
import type { Props as HeaderImageProps } from '@components/HeaderImage.astro'
import type { Props as BaseProps } from '@layouts/Base.astro'
import Base from '@layouts/Base.astro'
import Header from '@components/Header.astro'
import HeaderImage from '@components/HeaderImage.astro'
import Footer from '@components/Footer.astro'

export interface Props extends BaseProps {
  headerImage?: string
  headerImagePosition?: HeaderImageProps['imagePosition']
}

const { headerImage, headerImagePosition, ...props } = Astro.props
---

<Base {...props}>
  {
    headerImage && Astro.slots.has('header-title') ? (
      <HeaderImage image={headerImage} imagePosition={headerImagePosition}>
        <slot name="header-title" />
      </HeaderImage>
    ) : (
      <Header />
    )
  }
  <slot />
  <Footer />
</Base>
